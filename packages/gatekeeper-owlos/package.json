{
  "name": "@gadgets/owlos-gatekeeper",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/owlos.ts",
  "scripts": {
    "dev": "echo \"run 'pnpm dev-server' in the root directory instead\" >&2 && exit 1",
    "deploy": "wrangler deploy",
    "test": "node --import tsx --test src/owlos-store.test.ts",
    "clean": "rm -rf dist src/generated"
  },
  "dependencies": {
    "@gadgets/workshop-shared": "workspace:*",
    "capnweb": "catalog:",
    "capnweb-validate": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "wrangler": "catalog:"
  }
}
