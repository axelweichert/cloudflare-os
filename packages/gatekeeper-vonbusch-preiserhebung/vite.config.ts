// Vite+ per-package settings. Die `build:configurator`-Task-Definition wird von allen Gatekeepern
// mit `src/configurator/`-UI geteilt und liegt neben ihrem Builder.
export { default } from '../../scripts/gatekeeper-configurator-vite-config.js'
